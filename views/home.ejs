<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - MeetMyHomes</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }
    .logo img {
      max-height: 40px;
      max-width: 150px;
      height: auto;
      vertical-align: middle;
    }
    .hero {
      position: relative;
      text-align: center;
      padding: 6rem 1.5rem;
      background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('/images/home-hero-bg.jpg');
      background-size: cover;
      background-position: center;
      color: white;
      margin-bottom: 3rem;
      animation: fadeIn 1.5s ease-in-out;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: inherit;
      z-index: -1;
      filter: brightness(0.8);
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 700;
      color: #fff;
      margin-bottom: 1.5rem;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    .hero p {
      font-size: 1.3rem;
      color: #e0e0e0;
      max-width: 700px;
      margin: 0 auto 2.5rem;
      line-height: 1.6;
    }

    .hero .cta-button {
      display: inline-block;
      padding: 1rem 2.5rem;
      background-color: #f8f9fa;
      color: #2c3e50;
      text-decoration: none;
      border-radius: 25px;
      font-size: 1.2rem;
      font-weight: 600;
      transition: background-color 0.3s ease, transform 0.2s ease;
      border: 2px solid #79a9ee;
    }

    .hero .cta-button:hover {
      background-color: #dfe6e9;
      transform: translateY(-2px);
    }

    .properties-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 2.5rem;
      margin: 3rem 0;
      padding: 0 0.5rem;
    }

    .property-card {
      border: 1px solid #eaeaea;
      border-radius: 12px;
      overflow: hidden;
      background: #ffffff;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .property-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
    }

    .property-image {
      width: 100%;
      height: 250px;
      object-fit: cover;
      transition: opacity 0.3s ease;
    }

    .property-image:hover {
      opacity: 0.9;
    }

    .property-info {
      padding: 1.8rem;
      background: #f8f9fa;
      border-bottom-left-radius: 12px;
      border-bottom-right-radius: 12px;
    }

    .price {
      font-size: 1.7rem;
      font-weight: 700;
      color: #2c3e50;
      margin-bottom: 0.5rem;
    }

    .property-info h2 {
      font-size: 1.4rem;
      color: #2c3e50;
      margin: 0.5rem 0;
      font-weight: 600;
      line-height: 1.3;
    }

    .property-info p {
      font-size: 1rem;
      color: #34495e;
      margin: 0.3rem 0;
      line-height: 1.5;
    }

    .features {
      display: flex;
      flex-wrap: wrap;
      gap: 0.7rem;
      margin: 1.2rem 0 0.5rem;
    }

    .feature {
      background: #f8f9fa;
      padding: 0.4rem 1rem;
      border-radius: 20px;
      font-size: 0.95rem;
      color: #2c3e50;
      border: 1px solid #dfe6e9;
      transition: background-color 0.3s ease;
    }

    .feature:hover {
      background-color: #dfe6e9;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    .no-properties {
      text-align: center;
      font-size: 1.2rem;
      color: #717171;
      padding: 20px 0;
    }

    @media (max-width: 768px) {
      .properties-grid {
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 2rem;
        margin: 2rem 0;
      }

      .property-image {
        height: 200px;
      }

      .property-info {
        padding: 1.2rem;
      }

      .price {
        font-size: 1.5rem;
      }

      .property-info h2 {
        font-size: 1.2rem;
      }

      .hero {
        padding: 4rem 1rem;
      }

      .hero h1 {
        font-size: 2rem;
      }

      .hero p {
        font-size: 1.1rem;
      }

      .hero .cta-button {
        font-size: 1.1rem;
        padding: 0.8rem 2rem;
      }

      .property-card {
        border-radius: 10px;
      }

      .property-info {
        border-bottom-left-radius: 10px;
        border-bottom-right-radius: 10px;
      }
    }

    @media (max-width: 480px) {
      .container {
        padding: 0 1rem;
      }

      .properties-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }

      .property-image {
        height: 180px;
      }

      .property-info {
        padding: 1rem;
      }

      .hero {
        padding: 3rem 1rem;
      }

      .hero h1 {
        font-size: 1.8rem;
      }

      .hero p {
        font-size: 1rem;
      }

      .hero .cta-button {
        font-size: 1rem;
        padding: 0.7rem 1.5rem;
      }

      .property-card {
        border-radius: 8px;
      }

      .property-info {
        border-bottom-left-radius: 8px;
        border-bottom-right-radius: 8px;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main class="container">
    <section class="hero">
      <h1>Welcome to MeetMyHomes</h1>
      <p>Discover your perfect property with us. Explore a wide range of homes and terrains tailored to your needs.</p>
      <a href="/properties" class="cta-button">Explore Properties</a>
    </section>

    <h1>Featured Properties</h1>
    <section class="properties-grid">
      <!-- Property 1 -->
      <% let propertyIds = ["casa-lombo-salao", "terreno-atouguia", "terreno-picos"]; %>
      <% propertyIds.forEach(propertyId => { %>
        <% if (properties && properties[propertyId]) { %>
          <div class="property-card">
            <a href="/properties/<%= propertyId %>">
              <img src="<%= properties[propertyId].images[0] || '/images/fallback.jpg' %>" alt="<%= properties[propertyId].title || 'Property Image' %>" class="property-image" loading="lazy">
              <div class="property-info">
                <div class="price"><%= properties[propertyId].price || 'N/A' %></div>
                <h2><%= properties[propertyId].title || 'Untitled Property' %></h2>
                <p><%= properties[propertyId].location || 'Location N/A' %></p>
                <div class="features">
                  <% (properties[propertyId].features || []).forEach(feature => { %>
                    <span class="feature"><%= feature %></span>
                  <% }); %>
                </div>
              </div>
            </a>
          </div>
        <% } else { %>
          <p class="no-properties">Property data for <%= propertyId %> not available.</p>
        <% } %>
      <% }); %>
    </section>
    <div style="text-align: center; margin: 2rem 0;">
      <a href="/properties" class="cta-button">View More</a>
    </div>

    
  </main>

  <%- include('partials/footer') %>
</body>
</html>